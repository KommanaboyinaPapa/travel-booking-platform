export async function createPayment(req, res) {
  res.status(201).json({ message: 'Create payment placeholder' });
}

export async function listPayments(req, res) {
  res.json({ message: 'List payments placeholder' });
}
